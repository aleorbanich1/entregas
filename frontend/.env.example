# Proyecto Supabase COMPARTIDO con la app de Tareas (ref qsewancpibyyakitwpnr).
# Sólo la anon key en el cliente. NUNCA la service_role.
VITE_SUPABASE_URL=https://qsewancpibyyakitwpnr.supabase.co
VITE_SUPABASE_ANON_KEY=tu_anon_key

# Web Push (OPCIONAL). Clave PÚBLICA VAPID (la privada va sólo en la Edge Function).
# Si queda vacía, la app funciona igual sin push web.
VITE_VAPID_PUBLIC_KEY=
